#include <bits/stdc++.h>
using namespace std;
#define IOS ios::sync_with_stdio(false); cin.tie(nullptr);
#define lil long long
#define newline <<'\n';


int main() {
    IOS;

    int t, a, b, c, d;
    cin >> t ;
    while (t--) {
        cin >> a >> b >> c >> d;
        cout << b << " " << c << " " << c newline;
    }

    return 0;
}