#include <bits/stdc++.h>
using namespace std;
#define IOS ios::sync_with_stdio(false); cin.tie(nullptr);
#define lil long long int

void solve() {
    

}

int main() {
    IOS;

    int t;
    cin >> t;
    lil sum=0, temp;
    while (t--) {
        cin >> temp;
        sum+=temp;
    }
    cout << sum;
    return 0;
}